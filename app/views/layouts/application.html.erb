<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
	<meta name="keywords" content="Texas Unclaimed Property , Texas Money, My Money, Get My Money, where is my money, unclaimed property" />
	<meta name="description" content="ClaimVille - Where you search and claim your missing unclaimed money or property in Texas" />	
    <%= stylesheet_link_tag "/stylesheets/application.css" %>
	<%= stylesheet_link_tag "/javascripts/fancybox/jquery.fancybox-1.3.4.css"%>
    <%= javascript_include_tag :defaults %>
	<%= javascript_include_tag "/javascripts/fancybox/jquery.fancybox-1.3.4.pack.js", "/javascripts/fancybox/jquery.easing-1.3.pack.js", "/javascripts/fancybox/jquery.mousewheel-3.0.4.pack.js"%>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
	<%= render :partial => "layouts/ga"%>
  </head>
  <body>  		
  	<%= render :partial => "layouts/header", :locals => {:show_search => true} %>
    <div id="container">
		<% if current_user%>
			<div style="float:right;border:1px solid #000000;padding:10px;background-color:#CDD;text-align:right;">					
				<%=raw "You claimed <b>$#{totalclaimed.to_f}</b> -- till now"%><br>
				<%=raw "You found <b>$#{total_found_amount.to_f}</b> -- for your friends till now"%>
			</div>
		<% end%>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, title_text if show_title? %>
      <%= yield %>
    </div>
	<%= render :partial => "layouts/footer"%>	
  </body>
</html>
